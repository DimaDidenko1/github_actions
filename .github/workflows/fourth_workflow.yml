name: Fourth Workflow
on: 
    workflow_run:
      workflows: ["Third Workflow"]
      types:
        - completed

jobs:
    hello-world-command:
      runs-on: ubuntu-latest
      steps:
        - name: Hello World
          run: echo "Hello World!"
